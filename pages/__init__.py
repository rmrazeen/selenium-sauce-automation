"""
Turn Project into a Package (__init__.py)
Add an empty __init__.py file in pages/, tests/, and utils/.
Your project now behaves like a Python package.
This works because pytest automatically considers the project root as a package when __init__.py files are present.

"""

# Empty file